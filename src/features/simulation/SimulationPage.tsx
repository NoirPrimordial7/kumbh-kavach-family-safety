import { ArrowLeft, Expand, RotateCcw } from 'lucide-react';
import { brand } from '@/config/brand';

export function SimulationPage(){const scenario=new URLSearchParams(location.search).get('scenario');const src=`/legacy/kumbh-kavach-command-center.html${scenario?`?scenario=${encodeURIComponent(scenario)}`:''}`;return <main className="simulation-page"><header><button onClick={()=>location.assign('/')}><ArrowLeft/>Back to Family App</button><div><span className="legacy-tag">RESEARCH LAYER</span><strong>{brand.applicationName} · Crowd Command Center</strong></div><div><button onClick={()=>{const frame=document.querySelector<HTMLIFrameElement>('#legacy-frame');frame?.requestFullscreen()}}><Expand/>Open Full Screen</button><button onClick={()=>{const frame=document.querySelector<HTMLIFrameElement>('#legacy-frame');if(frame)frame.src=src}}><RotateCcw/>Reset Simulation</button></div></header><div className="simulation-notice">This unchanged legacy simulation explores future large-event infrastructure. Private family locations are not shared with event operators by default.</div><iframe id="legacy-frame" title="Legacy Kumbh Kavach Crowd Command Center" src={src} sandbox="allow-scripts allow-same-origin allow-forms allow-modals allow-downloads"/></main>}
